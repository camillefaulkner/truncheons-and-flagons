import { getPlayers } from "./dataAccess.js";

//print HTML for players of the chosen team

export const Players = () => {
    //if team.id = player.teamId, print HTML (find method)
    //needs event listener on teams
}